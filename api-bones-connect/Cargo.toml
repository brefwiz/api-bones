[package]
name = "api-bones-connect"
version = "0.1.1"
edition = "2024"
authors = ["Gregoire Salingue"]
license = "MIT"
description = "Connect RPC adapter primitives for api-bones services (ADR-0096)"
repository = "https://github.com/brefwiz/api-bones"
homepage = "https://github.com/brefwiz/api-bones"
documentation = "https://docs.rs/api-bones-connect"
readme = "README.md"
keywords = ["connect", "grpc", "rpc", "protobuf", "api-bones"]
categories = ["web-programming::http-server", "api-bindings", "network-programming"]
rust-version = "1.85"
# brefwiz-spiffe-client is published to the Brefwiz registry, and crates.io
# refuses a package whose dependencies are not themselves on crates.io. This
# crate therefore ships where its dependency does. Restore the crates.io
# default (remove this key) once brefwiz-spiffe-client is published publicly.
publish = ["brefwiz"]

[dependencies]
api-bones = { version = "6", path = "..", default-features = false, features = ["connect"] }
connectrpc = { version = "0.7", optional = false }
brefwiz-spiffe-client = { version = "0.43", registry = "brefwiz" }
http = "1"
thiserror = "2"
tokio = { version = "1", features = ["rt", "time"] }
tracing = "0.1"

[dev-dependencies]
tokio = { version = "1", features = ["rt", "macros", "time"] }

[lints]
workspace = true
